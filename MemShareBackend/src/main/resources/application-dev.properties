# Development Environment Configuration
spring.profiles.active=dev

# Database Configuration for Development
spring.datasource.url=jdbc:postgresql://localhost:5432/jwt_security_dev
spring.datasource.username=raghav
spring.datasource.password=Lincoln438917

# JPA Configuration for Development
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging for Development
logging.level.com.rag=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration for Development
jwt.secret=dev-secret-key-change-in-production
jwt.expiration=86400000

# AWS S3 Configuration for Development
aws.s3.bucket-name=memshare-dev-bucket
aws.s3.region=us-east-1

# Server Configuration
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always 